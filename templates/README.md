# Templates

Reusable request payloads live here.

These files are inputs that a user can copy or pass to the CLI as request templates, for example a
single-variant request JSON or a composite-profile CSV. Complete runnable configuration belongs in
`configs/`, not here.
