# End-to-end tests (Wave 1)
Full pipeline over the `tiny_cohort` fixture: assert counts, consent exclusion, SDC behaviour, the four artefacts, and that only the aggregate is airlock-export. Mark `@pytest.mark.e2e`.
