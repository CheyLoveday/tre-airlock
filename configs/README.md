# Configs

Runtime configuration belongs here.

- `../config.yaml` is the default runnable configuration used by the CLI, demo, and workflow.
- `examples/` contains complete alternative configs for documented demo runs.
- `catalogs/` contains runtime catalogues used by those configs, such as the demo variant/gene/panel catalogue.

These files are loaded by the package. They are not copy/paste templates.
